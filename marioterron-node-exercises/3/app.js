// read external file to get URL of external website
const read = require('./read');

read();
// parse HTML of external page

// request module is deprecated

// Request URL, return HTML method to get HTML code

//console.log(HTML.Method.return)

// return HTML code (ideally somewhat intact, structure-wise) within the console

